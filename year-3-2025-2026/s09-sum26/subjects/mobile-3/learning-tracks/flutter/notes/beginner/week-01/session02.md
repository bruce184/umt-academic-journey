Session: Session 2 - First Flutter App

Goal:
- Create the first Flutter project.
- Run the app successfully in the browser.
- Understand the basic structure of `main.dart`.
- Replace the default UI with my own content.

Output completed:
- Created and ran the first Flutter app successfully in `01_beginner/02_flutter_fundamentals/first_app`.
- Understood the roles of `main()`, `runApp()`, `MaterialApp`, `HomePage`, `Scaffold`, `AppBar`, `body`, `Center`, and `Column`.
- Replaced the default UI with a small exercise showing personal information.
- Used hot reload to update the UI quickly during practice.

Evidence:
- Related project: `01_beginner/02_flutter_fundamentals/first_app`
- Screenshot: Captured during the practice session while running on Chrome.

What I understood:
- `main()` is the entry point of the program.
- `runApp()` starts the root widget of the application.
- `MaterialApp` is the top-level app shell.
- `HomePage` is the first screen shown in the app.
- `Scaffold` provides the basic structure of a screen.
- `Column` arranges multiple widgets vertically.
- Hot reload updates the UI quickly and usually keeps the current state.
- Hot restart reruns the app more fully, almost from the beginning.

What is still unclear:
- I still do not deeply understand `StatefulWidget` and `setState`.
- I am not yet sure when I should split widgets into separate files.

What I need to review next session:
- Flutter mindset.
- `StatelessWidget` and `StatefulWidget`.
- Better widget organization.
