#include <bits/stdc++.h>
using namespace std;

int main(){
    int luckyNums1[] = {}; // empty array
    int luckyNums2[] = {4, 8, 15, 16, 23, 42}; // an array that already has values
    int luckyNums3[20]; // empty array with 20 is the size

    cout<<luckyNums2[0]<<endl; // 4
    luckyNums2[0] = 100;
    cout<<luckyNums2[0]; // 100

    return 0;
}
