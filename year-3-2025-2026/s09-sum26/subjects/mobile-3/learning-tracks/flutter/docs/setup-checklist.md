# Setup Checklist

- Install Flutter SDK on Windows.
- Install Dart and Flutter extensions in VS Code.
- Run `flutter doctor` and fix blocking issues first.
- Decide the main target for current study sessions: browser, Windows desktop, or emulator.
- Run `flutter analyze` before important commits once projects become larger.
