Session: Session 3 - Dart Basics 1

Goal:
- Learn variables, types, and null safety.
- Review basic `if`, `for`, `while`, and function syntax.
- Create a Dart file to practice syntax directly.

Output completed:
- Created the file `01_beginner/01_setup_dart_basics/exercises/basics_1/basic_1_practice.dart`.
- Added examples covering `var`, `final`, `const`, null safety, `if`, `for`, `while`, and basic functions.
- Prepared a practice file that can be edited by hand and run during the session.

Evidence:
- Related project: `01_beginner/01_setup_dart_basics/exercises/basics_1`
- Screenshot: to be added after the file is run successfully

What I understood:
- `var` lets Dart infer the variable type.
- `final` can be assigned once at runtime.
- `const` is a compile-time constant.
- A nullable variable uses `?`, for example `String?`.
- `if`, `for`, and `while` control program flow.
- Functions help separate and reuse logic.

What is still unclear:
- When `final` should be used instead of `var`.
- Which null safety cases need the most attention in real projects.

What I need to review next session:
- `List`, `Map`, and `Set`.
- Named parameters.
- Null-aware operators: `?.`, `??`, `!`.
