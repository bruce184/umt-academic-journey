// views/department-head/department-head.js
// Dùng layout chung cho avatar dropdown (và sidebar nếu sau này có)
import { initLayout } from "../common/layout-common.js";

document.addEventListener("DOMContentLoaded", () => {
  initLayout();

  // Chỗ này hiện chưa có logic riêng cho Department Head.
  // Nếu sau này thêm tab/feature riêng, đặt code tiếp ở đây.
});
