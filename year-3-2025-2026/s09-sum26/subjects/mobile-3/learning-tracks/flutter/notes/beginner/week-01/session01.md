Session: Session 1 - Environment Setup

Goal:
- Install Flutter by following the official VS Code instructions.
- Install the Flutter extension and Dart extension in VS Code.
- Run `flutter doctor` and fix the main blocking issues.

Output completed:
- Installed the Flutter SDK and configured the development environment on this machine.
- Installed the Flutter extension and Dart extension in VS Code.
- Successfully ran `flutter doctor`.
- Confirmed that VS Code recognizes Flutter commands and is ready to create and run projects.
- Recorded the issues that appeared during setup and how they were resolved.

What I understood:
- The Flutter SDK, Dart SDK, VS Code extensions, and target device all need to work together for the environment to be stable.
- `flutter doctor` is the most important command for quickly checking the overall environment status.
- Setup issues should be fixed early before moving on to building the first app.

What is still unclear:
- No major blockers remain after finishing the setup.
- If `flutter doctor` still shows minor warnings, I should keep an eye on them, but they do not block learning.

What I need to review next session:
- Recheck which target I will use to run the app: desktop or emulator.
- Create the first Flutter project and run it successfully.
- Clearly distinguish `hot reload` from `hot restart` at the start of Session 2.
