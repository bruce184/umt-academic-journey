## Docker

To create database from scratch, the following variables must be set:

```
POSTGRES_DB
POSTGRES_USER
POSTGRES_PASSWORD
POSTGRES_PORT
```

See [Environment variables](./env.md).

Then run:

```sh
docker compose up -d
```
